# ⚙️ Installation

Open **Termux** and enter:

```
pkg update -y
pkg upgrade -y
pkg install nodejs
pkg install git
git clone https://github.com/Cpmhaoproject/Termux-translate.git
cd Termux-translate
node Enc_autofy.js
```
